def get_odds(first=1, last=10, step=2):
    number = first
    while number <= last:
        yield number
        number += step


x = get_odds()
print(next(x))
print(next(x))
print(next(x))


for i in x:
    print(i)
